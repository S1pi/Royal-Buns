// Only a prototype, not used in the projects

type profileTranslations = {
  Heading: string;
  Name: string;
  Email: string;
  phonenumber: string;
  favourite_burger: string;
};

export type {profileTranslations};
